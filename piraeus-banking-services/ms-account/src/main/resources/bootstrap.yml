spring:
  application:
    name: ms-account
  cloud:
    config:
      uri: http://localhost:8889
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user

db:
  connection:
    url: jdbc:h2:tcp://localhost:9123/~/test
    port: 9123
    username: sa
    password:
